name: my_notes
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:   # ← paket localization bawaan Flutter
    sdk: flutter
  intl: any                # ← paket format teks internasional

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true           # ← aktifkan auto-generate dari ARB
  uses-material-design: true
